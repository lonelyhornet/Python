one = float(input("Enter first number: "))

two = float(input("Enter second number: "))

three = float(input("Enter third number: "))

four = float(input("Enter fourth number: "))

print('Calculating average...')
average = one + two + three + four
print("The average is: ")
print(average/4)

#Allows the input of 4 variables, converts them to a float on immediate input (float = decimal)

#Adds up all the variables, divides by 4 and prints.

